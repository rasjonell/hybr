# /home/rasjonell/.config/tmuxinator/hybr.yml

name: hybr
root: ~/Projects/hybr

windows:
  - Editor:
      layout: main-vertical
      panes:
        - lvim .

  - CLI:
      layout: main-vertical
      panes:
        - ls -al


  - Server:
      layout: main-vertical
      panes:
        - cd cmd/server && air

  - VPS:
      layout: main-vertical
      panes:
        - echo "TERM=linux ssh vultr"
